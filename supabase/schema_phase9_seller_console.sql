-- ============================================================
-- NOORMEXA — لوحة تحكم علامة تجارية احترافية (زي Amazon Seller)
-- ============================================================

alter table public.stores
  add column if not exists banner_url text;

-- ============================================================
-- ملحوظة: تعديل بيانات المتجر والمنتجات (الاسم، الوصف، السعر...)
-- كان مسموح بيه أصلًا لصاحب المتجر عن طريق سياسات RLS الموجودة
-- من قبل (stores_owner_update و products_owner_manage) — يعني
-- مفيش صلاحيات جديدة مطلوبة هنا، بس الواجهة نفسها كانت ناقصة.
-- ============================================================
